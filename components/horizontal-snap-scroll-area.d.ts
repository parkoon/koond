import { default as React } from '../../node_modules/react';

export type HorizontalSnapScrollAreaProps = {
    children: React.ReactNode;
    slidesToShow?: number;
    padding?: number;
};
export declare const HorizontalSnapScrollArea: ({ children, slidesToShow, padding, }: HorizontalSnapScrollAreaProps) => import("react/jsx-runtime").JSX.Element;
